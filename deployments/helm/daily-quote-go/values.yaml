replicaCount: 1

namespace: default

app:
  name: daily-quote-go
  chart: daily-quote-go-chart

image:
  repository: acrdevdailyquote.azurecr.io/daily-quote-go 

service:
  type: LoadBalancer
  port: 80
